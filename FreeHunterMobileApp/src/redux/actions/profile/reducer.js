import {} from './constants';
const initialState = {};
const ProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return { ...state };
    }
  }
};
export default ProfileReducer;
